name: Manual Eufy Control

on:
  workflow_dispatch:

jobs:
  run-eufy-task:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18' # Node 18 is the "sweet spot" for Eufy legacy support

      - name: Install Eufy Client
        run: npm install eufy-security-client --legacy-peer-deps

      - name: Run Control Script
        env:
          EUFY_EMAIL: ${{ secrets.EUFY_EMAIL }}
          EUFY_PASSWORD: ${{ secrets.EUFY_PASSWORD }}
        run: |
          echo "const { EufySecurity } = require('eufy-security-client');
          async function main() {
              try {
                  const eufy = new EufySecurity({
                      username: process.env.EUFY_EMAIL,
                      password: process.env.EUFY_PASSWORD,
                      country: 'US',
                  });
                  console.log('Connecting to Eufy...');
                  await eufy.connect();
                  
                  eufy.on('devices', (devices) => {
                      console.log('Successfully connected!');
                      console.log('Found ' + devices.length + ' devices.');
                      devices.forEach(d => console.log(' - ' + d.getName()));
                      process.exit(0);
                  });
              } catch (e) {
                  console.error('Script Error:', e.message);
                  process.exit(1);
              }
              setTimeout(() => { 
                  console.log('Timeout: Could not find devices. Check 2FA status.'); 
                  process.exit(1); 
              }, 30000);
          }
          main();" > run_script.js
          
          # We put the flags directly on the command here to bypass the restriction
          node --openssl-legacy-provider run_script.js
